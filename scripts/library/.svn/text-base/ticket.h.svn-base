#include<stdio.h>
#include<stdlib.h>

struct trip{
	int booked;
	struct trip * next;
};

extern void reserve(struct trip *stop, int depart, int arrival);
extern int check(struct trip *stop, int depart, int arrival);
